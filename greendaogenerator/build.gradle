apply plugin: 'java'

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile group: 'de.greenrobot', name: 'greendao-generator', version: '2.1.0'

}
tasks.withType(JavaCompile) {
    options.encoding = "UTF-8"
}